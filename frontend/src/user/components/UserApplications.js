import React from "react";

const UserApplications = () => {
  //We return a <AnnouncementsList> that the user applied to with a button CANCEL
  return <h2>Mes Postulations</h2>;
};

export default UserApplications;
